public class MyStepdefs {
    public MyStepdefs() {
        And("^I select the reason as \"([^\"]*)\"$", (String arg0) -> {
            // Write code here that turns the phrase above into concrete actions
            throw new cucumber.api.PendingException();
        });
    }
}
